
version: "3"
services:
  ptapiserver:
    build: .
    restart: always
    image: "www.greisslomat.at:5000/ptapi:1.0"
    ports:
      - "8060:8060"
    volumes:
      - /var/log/zvt:/var/log/zvt
    extra_hosts:
      - "pt:${PT_ADDR}"
